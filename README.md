# doc-booster-synchronizer

This project contains a service that is installed as a webhook on the documentation project to automaticlly create PRs,
when documentation for the boosters front-end is changed.
